import { useState } from "react"

const useCharacters = () => {
  const [characters, setCharacters] = useState([])

  // useEffect(() => {
//   getCharacters()
// }, [])

  function getCharacters () {
    await axios(dhsajgdhjas)
    await axios(dhsajgdhjas)
    await axios(dhsajgdhjas)
    setCharacters()
  }
}
